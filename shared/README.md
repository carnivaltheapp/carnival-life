# Shared Carnival code

This directory is reserved for capabilities that are genuinely shared by Carnival applications, such as domain types or external-service adapters once those capabilities are implemented.

Phase 0 intentionally contains no shared runtime package. A package should be introduced here only when PlayHouse, Roller, or another Carnival application has a concrete shared dependency.
